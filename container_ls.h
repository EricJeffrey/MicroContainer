#if !defined(CONTAINER_LS_H)
#define CONTAINER_LS_H

/**
 * @brief list containers
 * @param all list all containers if true, otherwise only running contaner
 */
void listContainer(bool all = false) noexcept;

#endif // CONTAINER_LS_H
